# Configuration parameters appearing at the top, not in a command prompt, will affect the entire file
# The following command sets the command prompt to $ for the entire file
#!! command_prompt = $


$ echo Change command prompt to $
Change command prompt to $


$ echo fin
fin
